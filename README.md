# § leatherbound

This is a custom new tab page for Google Chrome built on React. To install it:
1. Download the repository as a zip file and then unpack.
2. Go to `chrome://extensions/` and turn on developer mode.
3. Press the "Load Unpacked" button. Upload the `leatherbound-master/build` folder from the unpacked zip.
4. You're all set!
